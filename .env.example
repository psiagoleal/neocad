# .env.example — APENAS placeholders. NUNCA preencha com valores reais.
# Em repositório público, o .env real deve constar no .gitignore e no .claudeignore.

# USER:BEGIN id=projeto-env-vars
DATABASE_URL=postgresql://user:password@localhost:5432/dbname
API_KEY=your-key-here
# USER:END
